# 🚀 বট চালু করার জন্য সরাসরি কমান্ড

## ধাপ ১: SSH কানেক্ট করুন
```
ssh root@84.54.23.85
```
পাসওয়ার্ড দিন

## ধাপ ২: নিচের কমান্ড কপি করুন এবং পেস্ট করুন:

```bash
cd /root/diamond-bot && pm2 start index.js --name diamond-bot && pm2 start admin-panel/server.js --name admin-panel && pm2 save && pm2 list
```

এন্টার চাপুন।

---

## ✅ সফল হলে দেখবেন:

```
[PM2] Starting app index.js in fork mode...
[PM2] App started with pid 1234

[PM2] Starting app admin-panel/server.js in fork mode...
[PM2] App started with pid 1235

┌─────┬──────────────┬─────────┬─────────┬─────────┐
│ id  │ name         │ mode    │ status  │ uptime  │
├─────┼──────────────┼─────────┼─────────┼─────────┤
│ 0   │ diamond-bot  │ fork    │ online  │ 2s      │
│ 1   │ admin-panel  │ fork    │ online  │ 1s      │
└─────┴──────────────┴─────────┴─────────┴─────────┘
```

---

## 🌐 অ্যাডমিন প্যানেল খুলুন

**ব্রাউজারে:**
```
http://84.54.23.85:3000
```

**লগইন:**
- Username: `admin`
- Password: `secure_password_123`

---

## 📱 WhatsApp QR দেখতে

SSH-তে:
```bash
pm2 logs diamond-bot
```

QR কোড স্ক্যান করুন।

---

## 🆘 যদি সমস্যা হয়

**সব বন্ধ করুন:**
```bash
pm2 stop all
```

**সব রিস্টার্ট করুন:**
```bash
pm2 restart all
```

**স্ট্যাটাস দেখুন:**
```bash
pm2 list
```

**লাইভ লগ:**
```bash
pm2 logs
```
