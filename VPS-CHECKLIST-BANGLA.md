# ✅ VPS Bot চালু করার চেকলিস্ট

## 🎯 লক্ষ্য: বট এবং অ্যাডমিন প্যানেল VPS-এ চালু করা

---

## ✔️ ধাপ ১: SSH কানেক্ট করুন (১ মিনিট)

### PowerShell খুলুন:
```powershell
ssh root@84.54.23.85
```

**যখন পাসওয়ার্ড চাইবে:**
- Contabo ইমেইল থেকে পাসওয়ার্ড টাইপ করুন
- (প্যাসওয়ার্ড দৃশ্যমান নয়, ভয় পাবেন না)
- Enter চাপুন

**সফল হলে আপনি দেখবেন:**
```
root@vps-server:~#
```

---

## ✔️ ধাপ ২: একটি কমান্ড কপি-পেস্ট করুন (১ মিনিট)

SSH টার্মিনালে এটি কপি করুন এবং পেস্ট করুন:

```bash
cd /root/diamond-bot && pm2 start index.js --name diamond-bot && pm2 start admin-panel/server.js --name admin-panel && pm2 save && pm2 startup && pm2 list
```

**প্রেস Enter**

---

## ✔️ ধাপ ৩: সফলতা যাচাই করুন (১০ সেকেন্ড)

আপনি দেখবেন:
```
[PM2] Starting app index.js in fork mode...
[PM2] App started

[PM2] Starting app admin-panel/server.js in fork mode...
[PM2] App started
```

এবং একটি টেবিল:
```
┌─────┬──────────────┬─────┬─────────┬─────────┬──────────┐
│ id  │ name         │ ... │ status  │ restart │ uptime   │
├─────┼──────────────┼─────┼─────────┼─────────┼──────────┤
│ 0   │ diamond-bot  │ ... │ online  │ 0       │ 5s       │
│ 1   │ admin-panel  │ ... │ online  │ 0       │ 3s       │
└─────┴──────────────┴─────┴─────────┴─────────┴──────────┘
```

✅ **যদি দেখেন "online" - সাফল্য!**

---

## ✔️ ধাপ ৪: অ্যাডমিন প্যানেল খুলুন (৩০ সেকেন্ড)

### আপনার ব্রাউজারে:
```
http://84.54.23.85:3000
```

### লগইন করুন:
- **Username:** `admin`
- **Password:** `secure_password_123`

### ক্লিক করুন: **Home**

---

## ✔️ ধাপ ৫: স্টক সেট করুন (১ মিনিট)

1. **"স্টক সেট করুন"** বাটন ক্লিক করুন
2. সংখ্যা টাইপ করুন: `10000`
3. **"সংরক্ষণ করুন"** ক্লিক করুন

**সাফল্য:**
```
✅ স্টক সংরক্ষণ হয়েছে: 10,000 💎
```

---

## ✔️ ধাপ ৬: WhatsApp QR স্ক্যান করুন (২ মিনিট)

### SSH টার্মিনালে:
```bash
pm2 logs diamond-bot
```

**আপনি দেখবেন:**
- একটি QR কোড (টার্মিনালে আঁকা)
- বা একটি লিংক

**QR কোড স্ক্যান করুন:**
- WhatsApp খুলুন
- **Settings → Linked Devices** যান
- **"Link a device"** ক্লিক করুন
- **QR কোড স্ক্যান করুন**

**সফল হলে:**
```
✅ Client is ready!
✅ Bot initialized!
```

---

## ✔️ ধাপ ৭: পরীক্ষা করুন (২ মিনিট)

### WhatsApp গ্রুপে:
```
/d 100
```

**অ্যাডমিন দেখবে:**
- প্যানেলে নতুন অর্ডার দেখা যাবে
- "Pending" স্ট্যাটাস

**অনুমোদন করুন:**
- **অ্যাডমিন প্যানেলে** "Approve" বাটন ক্লিক করুন

**ফলাফল:**
```
✅ Stock: 10,000 → 9,900
✅ গ্রুপে নোটিফিকেশন পাঠানো হবে
```

---

## 🆘 সমস্যা সমাধান

### Q: "Permission denied" পেলাম?
**A:** পাসওয়ার্ড ভুল। পুনরায় চেষ্টা করুন।

### Q: "address already in use" পেলাম?
**A:** চালান:
```bash
pm2 delete all
pm2 start index.js --name diamond-bot
pm2 start admin-panel/server.js --name admin-panel
```

### Q: অ্যাডমিন প্যানেল লোড হচ্ছে না?
**A:** ৩০ সেকেন্ড অপেক্ষা করুন। তারপর:
```bash
pm2 restart admin-panel
```

### Q: কোনো লগ দেখা যাচ্ছে না?
**A:**
```bash
pm2 logs diamond-bot --lines 100
```

### Q: বট সাড়া দিচ্ছে না?
**A:**
```bash
pm2 logs diamond-bot --err
```

এবং দেখুন লাল লাইন।

---

## 📊 দৈনিক কমান্ড

| প্রয়োজন | কমান্ড |
|---------|--------|
| **সেবা চেক করতে** | `pm2 list` |
| **লাইভ লগ দেখতে** | `pm2 logs` |
| **সব পুনরায় চালু** | `pm2 restart all` |
| **রিয়েল-টাইম মনিটর** | `pm2 monit` |
| **সব বন্ধ করতে** | `pm2 stop all` |

---

## 🎉 সম্পন্ন!

✅ বট চালু  
✅ অ্যাডমিন প্যানেল চালু  
✅ স্টক সেট  
✅ WhatsApp কানেক্টেড  
✅ প্রথম অর্ডার পরীক্ষিত  

---

## 📞 এরপর?

1. **প্রতিদিন চেক করুন:** `pm2 logs`
2. **সাপ্তাহিক ব্যাকআপ:** `tar -czf backup.tar.gz /root/diamond-bot/config/`
3. **সমস্যা হলে:** লগ পাঠান

---

**আপনার বট এখন ২৪/৭ চলছে! 🚀💎**
