# 🚀 VPS Bot Manual Start (কারণ SSH পাসওয়ার্ড লুপ)

## দ্রুত সমাধান (২ মিনিট)

### ধাপ ১: SSH কানেক্ট করুন
```powershell
ssh root@84.54.23.85
```
এন্টার করুন পাসওয়ার্ড (Contabo ইমেইলে দেওয়া)

### ধাপ ২: নিচের সম্পূর্ণ কমান্ড কপি-পেস্ট করুন

```bash
cd /root/diamond-bot && pm2 start index.js --name diamond-bot && pm2 start admin-panel/server.js --name admin-panel && pm2 save && pm2 startup && pm2 list
```

### ধাপ ৩: অপেক্ষা করুন ২ সেকেন্ড

আপনি দেখবেন:
```
[PM2] Spawning daemon...
[PM2] PM2 daemon has been started successfully
```

### ধাপ ৪: স্ট্যাটাস চেক করুন
```bash
pm2 list
```

আপনি দেখবেন:
```
┌─────┬──────────────┬─────────┬─────────┬─────────┬──────────┐
│ id  │ name         │ version │ mode    │ status  │ restart  │
├─────┼──────────────┼─────────┼─────────┼─────────┼──────────┤
│ 0   │ diamond-bot  │ 0.0.0   │ fork    │ online  │ 0        │
│ 1   │ admin-panel  │ 0.0.0   │ fork    │ online  │ 0        │
└─────┴──────────────┴─────────┴─────────┴─────────┴──────────┘
```

### ধাপ ৫: সফল! আপনার সেবা চালু

**অ্যাডমিন প্যানেল অ্যাক্সেস করুন:**
- URL: `http://84.54.23.85:3000`
- Username: `admin`
- Password: `secure_password_123`

---

## 📱 WhatsApp QR কোড দেখতে

SSH-এ:
```bash
pm2 logs diamond-bot
```

আপনি দেখবেন QR কোড টার্মিনালে। এটি স্ক্যান করুন।

---

## 📊 লাইভ লগ দেখুন

### বট লগ
```bash
pm2 logs diamond-bot --lines 50
```

### অ্যাডমিন প্যানেল লগ
```bash
pm2 logs admin-panel --lines 50
```

### সব লগ রিয়েল-টাইম
```bash
pm2 monit
```

---

## 🔄 সেবা পুনরায় চালু করুন

```bash
pm2 restart all
```

## ⛔ সেবা বন্ধ করুন

```bash
pm2 stop all
```

## 🗑️ সেবা সম্পূর্ণ সরান

```bash
pm2 delete all
```

---

## 🛠️ সমস্যা সমাধান

### সেবা ক্র্যাশ হয়েছে?

```bash
# স্ট্যাটাস দেখুন
pm2 status

# লগ দেখুন
pm2 logs diamond-bot --err

# রিস্টার্ট করুন
pm2 restart diamond-bot
```

### পোর্ট ব্যস্ত?

```bash
# ব্যবহৃত পোর্ট দেখুন
netstat -tulpn | grep 3000
netstat -tulpn | grep 3001

# বিদ্যমান প্রসেস খুঁজুন
ps aux | grep node

# হত্যা করুন (যদি প্রয়োজন)
kill -9 <PID>
```

### npm ত্রুটি?

```bash
# পুনরায় ইনস্টল করুন
cd /root/diamond-bot
rm -rf node_modules package-lock.json
npm install --legacy-peer-deps
```

---

## 💾 ডাটাবেস ব্যাকআপ

```bash
# সম্পূর্ণ ব্যাকআপ তৈরি করুন
tar -czf /root/backup-$(date +%Y%m%d-%H%M%S).tar.gz /root/diamond-bot/config/

# ব্যাকআপ ডাউনলোড করুন (আপনার মেশিন থেকে)
scp root@84.54.23.85:/root/backup-*.tar.gz ./
```

---

## ✅ স্টার্টআপ রেজিস্ট্রেশন

সার্ভার রিবুট হলে স্বয়ংক্রিয় শুরু করতে:

```bash
pm2 startup
pm2 save
```

এটি একবার যথেষ্ট।

---

## 📝 দ্রুত কমান্ড চিট

| কমান্ড | কাজ |
|--------|------|
| `pm2 list` | সেবা স্ট্যাটাস |
| `pm2 restart all` | সব পুনরায় চালু |
| `pm2 stop all` | সব বন্ধ |
| `pm2 logs` | লাইভ লগ |
| `pm2 monit` | মনিটর (রিয়েল-টাইম) |
| `pm2 save` | সংরক্ষণ |
| `pm2 startup` | অটো-স্টার্ট |

---

## 🎉 সম্পূর্ণ!

আপনার বট এখন চলছে! 

**পরবর্তী:**
1. ✅ অ্যাডমিন প্যানেল খুলুন
2. ✅ স্টক সেট করুন
3. ✅ WhatsApp গ্রুপে পরীক্ষা করুন
4. ✅ অর্ডার অনুমোদন করুন

---

**সাহায্যের জন্য লগ দেখুন:** `pm2 logs`
